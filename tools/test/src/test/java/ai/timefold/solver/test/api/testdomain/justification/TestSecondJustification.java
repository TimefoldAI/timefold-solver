package ai.timefold.solver.test.api.testdomain.justification;

import ai.timefold.solver.core.api.score.stream.ConstraintJustification;

public record TestSecondJustification(String id) implements ConstraintJustification {
}
