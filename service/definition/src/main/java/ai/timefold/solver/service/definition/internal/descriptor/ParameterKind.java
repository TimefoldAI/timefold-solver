package ai.timefold.solver.service.definition.internal.descriptor;

public enum ParameterKind {
    /**
     * A constraint weight influencing the score calculation.
     */
    WEIGHT,
    /**
     * A general constraint parameter influencing the behavior of the constraint.
     */
    PARAMETER,
}
