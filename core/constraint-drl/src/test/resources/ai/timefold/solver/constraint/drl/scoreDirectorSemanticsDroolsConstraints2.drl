package packageOverwrittenOnField;
dialect "java"

import ai.timefold.solver.core.api.score.buildin.simple.SimpleScoreHolder;
import ai.timefold.solver.core.impl.testdata.domain.TestdataEntity;

global SimpleScoreHolder scoreHolder;

rule "Second weight"
when
    TestdataEntity()
then
    // Don't do anything. Only exists so that Timefold doesn't fail fast on constraint configuration.
end
