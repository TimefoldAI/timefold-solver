package ai.timefold.solver.core.impl.bavet.common;

public enum StreamKind {
    FOR_EACH,
    FILTER,
    JOIN,
    GROUP_BY,
    FLATTEN,
    MAP,
    PRECOMPUTE,
    IF_EXISTS,
    SCORING,
    CONCAT
}
