name: Quickstarts Workflow (disabled in fork)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled in this fork. Use the manual Publish to GitHub Packages workflow."
