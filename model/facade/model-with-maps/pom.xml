<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>ai.timefold.solver</groupId>
    <artifactId>timefold-solver-model-internal-parent</artifactId>
    <version>${revision}</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>

  <artifactId>timefold-solver-model-with-maps</artifactId>
  <name>(Preview) Timefold Solver Model with maps integration</name>
  <description>
    This module is a dependency aggregator for models that require maps integration.
    This module is in a preview state and thus is a subject to changes.
  </description>

  <dependencies>
    <dependency>
      <groupId>ai.timefold.solver</groupId>
      <artifactId>timefold-solver-model</artifactId>
    </dependency>
    <dependency>
      <groupId>ai.timefold.solver</groupId>
      <artifactId>timefold-solver-model-maps-service-rest</artifactId>
    </dependency>    
    <dependency>
      <groupId>ai.timefold.solver</groupId>
      <artifactId>timefold-solver-model-maps-service-client</artifactId>
    </dependency>
  </dependencies>
</project>
